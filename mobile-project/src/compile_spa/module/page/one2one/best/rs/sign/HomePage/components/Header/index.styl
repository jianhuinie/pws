@import 'compile_spa/resource/css/mixin/position.styl';
.sign-home-header {
    position: relative;
    padding-bottom: 37.33%;
    img {
        position: absolute;
        width: 100%;
        height: 100%;
    }
    span {
        v-center(absolute);
        font-size: rem(30);
        color: #FFFFFF;
        width: 100%;
        display: inline-block;
        text-align: center;
    }
}
