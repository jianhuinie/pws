.choose-content {
    position: fixed;
    left: 0;
    right: 0;
    top: rem(40);
    z-index: 10;
    background: #fff;
    &.hide {
        display: none;
    }
    .choose-content-item {
            height: rem(300);
            overflow: scroll;
    }
}