.footer {
    div {
        float: left;
        width: 50%;
        height: rem(50);
        line-height: rem(50);
    }
    .free-consult {
        font-size: rem(17);
        color: #3D3D3E;
    }
    .to-application {
        font-size: rem(18);
        color: #F61B11;
    }
}
.footer_fixed {
    position: fixed;
    bottom: 0;
    left: 0;
    background: #F8F8F8;
    right: 0;
}