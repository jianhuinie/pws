@import "../../../resource/css/default.styl";

.tag-image {
    position: relative;
    &-text {
        position: absolute;
        width: 16px;
        height: 12px;
        opacity: 0.6;
        background: $black;
        font-size: 12px;
        color: $white;
        line-height: 12px;
        left: 0;
        top: 0;
        text-align: center;
    }
}   