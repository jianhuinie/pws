/**
 * @file 搜狗导航页-学习头条all
 * @author jixiaohui
 */
define(function (require, exports) {

    'use strict';

    var container = $('#study-all');

    exports.init = function () {

        /*container
        .on('mouseenter', '.nav-box li', function(e){
            var element = $(this);
            var cat = element.data('cat');

            container.find('.nav-box li').removeClass('active');
            element.addClass('active');
            container.find('.list-box').hide();
            container.find('.content .'+cat).show();
        })*/
    };

});